# gd-wordpress-harden
Lightweight WordPress must-use plugin for agency-standard hardening. Disables comments, restricts REST API with plugin namespace whitelist, blocks XML-RPC abuse, removes emoji scripts, disables author archives and application passwords, strips version info, and warns when noindex is active. All features toggleable via wp-config.php.
